import { ClockViewTransitionDirective } from './clock-view-transition.directive';

describe('ClockViewTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new ClockViewTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
