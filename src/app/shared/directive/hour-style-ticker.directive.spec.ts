import { HourStyleTickerDirective } from './hour-style-ticker.directive';

describe('HourStyleTickerDirective', () => {
  it('should create an instance', () => {
    const directive = new HourStyleTickerDirective();
    expect(directive).toBeTruthy();
  });
});
