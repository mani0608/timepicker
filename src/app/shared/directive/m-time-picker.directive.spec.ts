import { MTimePickerDirective } from './m-time-picker.directive';

describe('MTimePickerDirective', () => {
  it('should create an instance', () => {
    const directive = new MTimePickerDirective();
    expect(directive).toBeTruthy();
  });
});
