import { MClockArrayPipe } from '../../m-clock-array.pipe';

describe('MClockArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new MClockArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
